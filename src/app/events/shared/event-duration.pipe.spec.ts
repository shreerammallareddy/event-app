import { EventDurationPipe } from './event-duration.pipe';

describe('EventDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new EventDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
